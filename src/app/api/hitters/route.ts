import { NextResponse } from 'next/server';

export const GET = async (): Promise<NextResponse> => {
  // Phase 3 placeholder — hitter matchup data built in next iteration
  return NextResponse.json({ hitters: [] });
};
